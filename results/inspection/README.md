# Inspection Outputs

This directory holds non-headline exploratory artifacts that should not live in the repository root.

- `eda/`: exploratory graphics such as distribution and correlation plots
- `logs/`: inspection and diagnostic text logs

These files are useful for development and auditing, but they are separate from the paper-facing outputs in the main `results/` directory.
